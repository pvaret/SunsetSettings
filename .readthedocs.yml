version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"

python:
  # Install our python package before building the docs.
  install:
    - method: pip
      path: .[docs]

sphinx:
  configuration: docs/source/conf.py

formats: all
